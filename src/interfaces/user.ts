// Re-export from interfaces for backward compatibility
export type { IUser, LoginCredentials, AuthResponse, CreateUserData, UpdateUserData } from '@/interfaces';
export { UserRole } from '@/interfaces';
